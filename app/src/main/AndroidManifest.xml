<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- TV Features -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />

    <!-- PiP Support -->
    <uses-feature
        android:name="android.software.picture_in_picture"
        android:required="false" />

    <!-- Package visibility for YouTube players (Android 11+) -->
    <queries>
        <!-- SmartTube Beta -->
        <package android:name="org.smarttube.beta" />
        <!-- YouTube TV Official -->
        <package android:name="com.google.android.youtube.tv" />
        <!-- Standard YouTube (fallback) -->
        <package android:name="com.google.android.youtube" />
        <!-- Allow VIEW intents for any video player -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="vnd.youtube" />
        </intent>
    </queries>

    <application
        android:name=".WaveStreamApplication"
        android:allowBackup="true"
        android:banner="@drawable/app_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WaveStream"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Main Activity (content browsing) - Jetpack Compose -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|navigation" />

        <!-- Profile Selection Activity (App Entry Point) -->
        <activity
            android:name=".ui.profile.ProfileSelectionActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Player Activity with PiP -->
        <activity
            android:name=".ui.seriea.SerieAActivity"
            android:exported="false"
            android:theme="@style/Theme.WaveStream" />
        <activity
            android:name=".ui.seriea.MatchDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.WaveStream" />
        <activity
            android:name=".ui.seriea.TeamDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.WaveStream" />
        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:screenOrientation="landscape"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.WaveStream.Player" />

        <!-- Details Activity -->
        <activity
            android:name=".ui.details.DetailsActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Search Activity -->
        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- EPG Activity -->
        <activity
            android:name=".ui.epg.EPGActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Category Activity (grid view for favorite categories) -->
        <activity
            android:name=".ui.category.AllCategoriesActivity"
            android:exported="false"
            android:theme="@style/Theme.WaveStream" />
        <activity
            android:name=".ui.category.CategoryActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Live TV Activity -->
        <activity
            android:name=".ui.live.LiveActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Multiscreen Activity -->
        <activity
            android:name=".ui.multiscreen.MultiscreenActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.WaveStream.Player" />

        <!-- Film Activity -->
        <activity
            android:name=".ui.film.FilmActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Series Activity -->
        <activity
            android:name=".ui.series.SeriesActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Setup Activity -->
        <activity
            android:name=".ui.setup.SetupActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Loading Activity -->
        <activity
            android:name=".ui.loading.LoadingActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Downloads Activity -->
        <activity
            android:name=".ui.downloads.DownloadsActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.WaveStream.FullScreen" />

        <!-- Video Download Service (ExoPlayer) -->
        <service
            android:name=".service.VideoDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- EPG Sync Worker -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
