<?xml version="1.0" encoding="utf-8"?>
<!-- Expanded progress bar for seeking mode (6dp height) -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Background - more visible when seeking -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="#50FFFFFF" />
            <corners android:radius="3dp" />
            <size android:height="6dp" />
        </shape>
    </item>
    
    <!-- Secondary progress (buffered) -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="#70FFFFFF" />
                <corners android:radius="3dp" />
                <size android:height="6dp" />
            </shape>
        </clip>
    </item>
    
    <!-- Primary progress (played) - bright accent color -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="#E50914" />
                <corners android:radius="3dp" />
                <size android:height="6dp" />
            </shape>
        </clip>
    </item>
    
</layer-list>
